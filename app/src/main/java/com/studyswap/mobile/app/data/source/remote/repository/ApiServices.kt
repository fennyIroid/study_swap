package com.studyswap.mobile.app.data.source.remote.repository

import retrofit2.Response

interface ApiServices {
    // Add suspend API methods returning Response<T> as needed
    // Example: suspend fun login(@Body req: LoginRequest): Response<AuthResponse>
}
