package com.studyswap.mobile.app.data.source

object Constants {
    object AppScreen {
        // Add screen keys as needed, e.g. const val HOME = "home"
    }

    object DataStore {
        const val PREFERENCE_NAME = "study_swap_preference"
    }
}
